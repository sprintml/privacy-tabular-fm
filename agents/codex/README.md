# Codex (GPT-5.5) agentic baseline

Harness for running Codex with GPT-5.5 as an autonomous privacy-attack
agent against the competition's TFM targets. Same sandbox and time
budget as the Claude Code harness; allows direct comparison between
the two frontier-agent baselines.

