# Per-track natural-language task prompts

The natural-language task specifications given to the agents at the
start of each trial. One prompt per track (attribute inference,
membership inference, dataset inference, property inference); each
describes the attack objective, available data, target-model access,
and the scoring metric.

